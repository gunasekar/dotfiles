gui:
  mouseEvents: true
  showDivergenceFromBaseBranch: arrowAndNumber

refresher:
  refreshInterval: 5

notARepository: skip

git:
  pagers:
    - colorArg: always
      pager: delta --paging=never --side-by-side
